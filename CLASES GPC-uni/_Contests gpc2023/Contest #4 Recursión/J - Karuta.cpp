#include <bits/stdc++.h>
#define all(x) x.begin(), x.end()

using namespace std;

char alp[];

int f(int pos, vector<int> idx) {
    for (int i : idx) {
        // vole
        cout << "oha";
    }
    return 0;
}

int main() {
    int n; cin >> n;
    vector<string> a(n);

    for (int i=0; i<n; i++) cin >> a[i];


    return 0;
}